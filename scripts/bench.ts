// CLI experiment matrix → CSV in docs/results/ and a summary table in docs/experiments.md.
// Implemented in Phase 4 (see PLAN.md §8.5).
console.error('bench: not implemented yet (Phase 4).');
process.exit(1);
